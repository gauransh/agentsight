[ext]
name = "web"
version = "1.0.25"
description = "Built-in AgentSight presentation extension."

[frontend]
components = "components"

# The trusted frontend shell owns authentication, Node selection, build-time
# composition, and capability approval. Product presentation lives here so it
# can evolve independently from that shell.
